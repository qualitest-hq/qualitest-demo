package ${packageName}.service.impl;

import cn.hutool.core.util.IdUtil;
import cn.hutool.core.util.StrUtil;
import com.demo.common.utils.SecurityUtils;
import java.util.List;
import java.util.Objects;
#foreach ($column in $columns)
#if($column.javaField == 'createTime' || $column.javaField == 'updateTime')
import com.demo.common.utils.DateUtils;
#break
#end
#end
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
#if($table.sub)
import java.util.ArrayList;
import com.demo.common.utils.StringUtils;
import org.springframework.transaction.annotation.Transactional;
import ${packageName}.domain.${subClassName};
#end
import ${packageName}.mapper.${ClassName}Mapper;
import ${packageName}.domain.${ClassName};
import ${packageName}.params.${ClassName}Params;
import ${packageName}.apiParams.${ClassName}ApiParams;
import ${packageName}.result.${ClassName}Result;
import ${packageName}.apiResult.${ClassName}ApiResult;
import ${packageName}.service.I${ClassName}Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * ${functionName}Service业务层处理
 * 
 * @author ${author}
 * @date ${datetime}
 */
@Service
public class ${ClassName}ServiceImpl implements I${ClassName}Service {
    @Autowired
    private ${ClassName}Mapper ${className}Mapper;

    /**
     * 查询${functionName}列表
     *
     * @param ${className} ${functionName}
     * @return ${functionName}
     */
    @Override
    public List<${ClassName}> select${ClassName}List(${ClassName} ${className}) {
        return ${className}Mapper.select${ClassName}List(${className});
    }

    /**
     * 查询${functionName}
     *
     * @param ${pkColumn.javaField} ${functionName}主键
     * @return ${functionName}
     */
    @Override
    public ${ClassName} select${ClassName}ById(${pkColumn.javaType} ${pkColumn.javaField}) {
        return ${className}Mapper.select${ClassName}ById(${pkColumn.javaField});
    }

    /**
     * 查询${functionName}Result列表
     *
     * @param params ${functionName}Params
     * @return ${functionName}Result集合
     */
    @Override
    public List<${ClassName}Result> select${ClassName}ResultList(${ClassName}Params params) {
        return ${className}Mapper.select${ClassName}ResultList(params);
    }

    /**
     * 获取${functionName}详细信息
     *
     * @param ${pkColumn.javaField} ${functionName}主键
     * @return ${functionName}Result
     */
    @Override
    public ${ClassName}Result select${ClassName}Result(${pkColumn.javaType} ${pkColumn.javaField}) {
        return ${className}Mapper.select${ClassName}Result(${pkColumn.javaField});
    }

    /**
     * 查询${functionName}ApiResult列表（客户端）
     */
    @Override
    public List<${ClassName}ApiResult> select${ClassName}ApiResultList(${ClassName}ApiParams params) {
        return ${className}Mapper.select${ClassName}ApiResultList(params);
    }

    /**
     * 获取${functionName}ApiResult详细信息（客户端）
     */
    @Override
    public ${ClassName}ApiResult select${ClassName}ApiResult(${pkColumn.javaType} ${pkColumn.javaField}) {
        return ${className}Mapper.select${ClassName}ApiResult(${pkColumn.javaField});
    }

    /**
     * 新增${functionName}
     *
     * @param ${className} ${functionName}
     * @return 结果
     */
    @Transactional(rollbackFor = Exception.class)
    @Override
    public int insert${ClassName}(${ClassName} ${className}) {
#if($pkColumn.columnType == 'bigint')
        if (Objects.isNull(${className}.get${pkColumn.capJavaField}())) {
            ${className}.set${pkColumn.capJavaField}(IdUtil.getSnowflakeNextId());
        }
#end
#if($pkColumn.columnType == 'varchar')
        if (StrUtil.isEmpty(${className}.get${pkColumn.capJavaField}())) {
            ${className}.set${pkColumn.capJavaField}(IdUtil.getSnowflakeNextIdStr());
        }
#end
#foreach ($column in $columns)
#if($column.javaField == 'createBy')
        ${className}.setCreateBy(SecurityUtils.getUserId().toString());
#end
#if($column.javaField == 'createTime')
        ${className}.setCreateTime(DateUtils.getNowDate());
#end
#end
#if($table.sub)
        int rows = ${className}Mapper.insert${ClassName}(${className});
        insert${subClassName}(${className});
        return rows;
#else
        return ${className}Mapper.insert${ClassName}(${className});
#end
    }

    /**
     * 修改${functionName}
     *
     * @param ${className} ${functionName}
     * @return 结果
     */
    @Transactional(rollbackFor = Exception.class)
    @Override
    public int update${ClassName}(${ClassName} ${className}) {
#foreach ($column in $columns)
#if($column.javaField == 'updateBy')
        ${className}.setUpdateBy(SecurityUtils.getUserId().toString());
#end
#if($column.javaField == 'updateTime')
        ${className}.setUpdateTime(DateUtils.getNowDate());
#end
#end
#if($table.sub)
        ${className}Mapper.delete${subClassName}By${subTableFkClassName}(${className}.get${pkColumn.capJavaField}());
        insert${subClassName}(${className});
#end
        return ${className}Mapper.update${ClassName}(${className});
    }

    /**
     * 批量删除${functionName}
     * 
     * @param ${pkColumn.javaField}List 需要删除的${functionName}主键集合
     * @return 结果
     */
#if($table.sub)
    @Transactional
#end
    @Override
    public int delete${ClassName}ByIdList(List<${pkColumn.javaType}> ${pkColumn.javaField}List) {
#if($table.sub)
        ${className}Mapper.delete${subClassName}By${subTableFkClassName}s(${pkColumn.javaField}List.toArray(new ${pkColumn.javaType}[0]));
#end
        return ${className}Mapper.delete${ClassName}ByIdList(${pkColumn.javaField}List);
    }

    /**
     * 删除${functionName}信息
     * 
     * @param ${pkColumn.javaField} ${functionName}主键
     * @return 结果
     */
#if($table.sub)
    @Transactional
#end
    @Override
    public int delete${ClassName}ById(${pkColumn.javaType} ${pkColumn.javaField}) {
#if($table.sub)
        ${className}Mapper.delete${subClassName}By${subTableFkClassName}(${pkColumn.javaField});
#end
        return ${className}Mapper.delete${ClassName}ById(${pkColumn.javaField});
    }
#if($logicDelete)

    /**
     * 逻辑删除${functionName}信息
     * 
     * @param ${pkColumn.javaField} ${functionName}主键
     * @return 结果
     */
    @Override
    public int logicDelete${ClassName}ById(${pkColumn.javaType} ${pkColumn.javaField}) {
        return ${className}Mapper.logicDelete${ClassName}ById(${pkColumn.javaField});
    }

    /**
     * 批量逻辑删除${functionName}信息
     * 
     * @param ${pkColumn.javaField}List ${functionName}主键集合
     * @return 结果
     */
    @Override
    public int logicDelete${ClassName}ByIdList(List<${pkColumn.javaType}> ${pkColumn.javaField}List) {
        return ${className}Mapper.logicDelete${ClassName}ByIdList(${pkColumn.javaField}List);
    }
#end

    /**
     * 查询${functionName}数量
     *
     * @param params ${functionName}Params
     * @return 数量
     */
    @Override
    public int select${ClassName}Count(${ClassName}Params params) {
        return ${className}Mapper.select${ClassName}Count(params);
    }

    /**
     * 按条件查询单条${functionName}
     *
     * @param params ${functionName}Params
     * @return ${functionName}
     */
    @Override
    public ${ClassName} select${ClassName}One(${ClassName}Params params) {
        return ${className}Mapper.select${ClassName}One(params);
    }
#if($table.sub)

    /**
     * 新增${subTable.functionName}信息
     * 
     * @param ${className} ${functionName}对象
     */
    public void insert${subClassName}(${ClassName} ${className}) {
        List<${subClassName}> ${subclassName}List = ${className}.get${subClassName}List();
        ${pkColumn.javaType} ${pkColumn.javaField} = ${className}.get${pkColumn.capJavaField}();
        if (StringUtils.isNotNull(${subclassName}List)) {
            List<${subClassName}> list = new ArrayList<${subClassName}>();
            for (${subClassName} ${subclassName} : ${subclassName}List) {
                ${subclassName}.set${subTableFkClassName}(${pkColumn.javaField});
                list.add(${subclassName});
            }
            if (list.size() > 0) {
                ${className}Mapper.batch${subClassName}(list);
            }
        }
    }
#end
}
