package ${packageName}.mapper;

import java.util.List;
import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;
import ${packageName}.domain.${ClassName};
import ${packageName}.params.${ClassName}Params;
import ${packageName}.apiParams.${ClassName}ApiParams;
import ${packageName}.result.${ClassName}Result;
import ${packageName}.apiResult.${ClassName}ApiResult;
#if($table.sub)
import ${packageName}.domain.${subClassName};
#end

/**
 * ${functionName}Mapper接口
 * 
 * @author ${author}
 * @date ${datetime}
 */
@Mapper
public interface ${ClassName}Mapper {
    /**
     * 查询${functionName}列表
     *
     * @param ${className} ${functionName}
     * @return ${functionName}集合
     */
    List<${ClassName}> select${ClassName}List(${ClassName} ${className});

    /**
     * 查询${functionName}
     *
     * @param ${pkColumn.javaField} ${functionName}主键
     * @return ${functionName}
     */
    ${ClassName} select${ClassName}ById(${pkColumn.javaType} ${pkColumn.javaField});

    /**
     * 查询${functionName}Result列表
     *
     * @param params ${functionName}Params
     * @return ${functionName}Result集合
     */
    List<${ClassName}Result> select${ClassName}ResultList(${ClassName}Params params);

    /**
     * 获取${functionName}详细信息
     *
     * @param ${pkColumn.javaField} ${functionName}主键
     * @return ${functionName}Result
     */
    ${ClassName}Result select${ClassName}Result(${pkColumn.javaType} ${pkColumn.javaField});

    /**
     * 查询${functionName}ApiResult列表（客户端）
     *
     * @param params ${functionName}ApiParams
     * @return ${functionName}ApiResult集合
     */
    List<${ClassName}ApiResult> select${ClassName}ApiResultList(${ClassName}ApiParams params);

    /**
     * 获取${functionName}ApiResult详细信息（客户端）
     *
     * @param ${pkColumn.javaField} ${functionName}主键
     * @return ${functionName}ApiResult
     */
    ${ClassName}ApiResult select${ClassName}ApiResult(${pkColumn.javaType} ${pkColumn.javaField});

    /**
     * 查询${functionName}数量
     *
     * @param params ${functionName}Params
     * @return 数量
     */
    int select${ClassName}Count(${ClassName}Params params);

    /**
     * 按条件查询单条${functionName}
     *
     * @param params ${functionName}Params
     * @return ${functionName}
     */
    ${ClassName} select${ClassName}One(${ClassName}Params params);

    /**
     * 新增${functionName}
     * 
     * @param ${className} ${functionName}
     * @return 结果
     */
    int insert${ClassName}(${ClassName} ${className});

    /**
     * 修改${functionName}
     * 
     * @param ${className} ${functionName}
     * @return 结果
     */
    int update${ClassName}(${ClassName} ${className});

    /**
     * 删除${functionName}
     * 
     * @param ${pkColumn.javaField} ${functionName}主键
     * @return 结果
     */
    int delete${ClassName}ById(${pkColumn.javaType} ${pkColumn.javaField});

    /**
     * 批量删除${functionName}
     * 
     * @param ${pkColumn.javaField}List 需要删除的数据主键集合
     * @return 结果
     */
    int delete${ClassName}ByIdList(@Param("list") List<${pkColumn.javaType}> ${pkColumn.javaField}List);
#if($logicDelete)

    /**
     * 逻辑删除${functionName}
     * 
     * @param ${pkColumn.javaField} ${functionName}主键
     * @return 结果
     */
    int logicDelete${ClassName}ById(${pkColumn.javaType} ${pkColumn.javaField});

    /**
     * 批量逻辑删除${functionName}
     * 
     * @param ${pkColumn.javaField}List ${functionName}主键集合
     * @return 结果
     */
    int logicDelete${ClassName}ByIdList(@Param("list") List<${pkColumn.javaType}> ${pkColumn.javaField}List);
#end
#if($table.sub)

    /**
     * 批量删除${subTable.functionName}
     * 
     * @param ${pkColumn.javaField}s 需要删除的数据主键集合
     * @return 结果
     */
    int delete${subClassName}By${subTableFkClassName}s(${pkColumn.javaType}[] ${pkColumn.javaField}s);
    
    /**
     * 批量新增${subTable.functionName}
     * 
     * @param ${subclassName}List ${subTable.functionName}列表
     * @return 结果
     */
    int batch${subClassName}(List<${subClassName}> ${subclassName}List);
    

    /**
     * 通过${functionName}主键删除${subTable.functionName}信息
     * 
     * @param ${pkColumn.javaField} ${functionName}ID
     * @return 结果
     */
    int delete${subClassName}By${subTableFkClassName}(${pkColumn.javaType} ${pkColumn.javaField});
#end
}
