# demo-ui

质衡（QualiTest）演示环境前端：Vue 3 + Element Plus + Vite。

## 前端运行

```bash
# 进入项目目录
cd demo-ui

# 安装依赖
yarn --registry=https://registry.npmmirror.com

# 启动服务
yarn dev

# 构建生产环境
yarn build:prod
```

建议使用开发构建，预览生产构建可用 `yarn preview`。
